# XY 桌面播放器瘦身报告

执行日期：2026-09-01

## 输入意见检查

用户提供的 `C:\Users\bill_\Desktop\项目瘦身.md` 存在，但审计时大小为 0 字节，没有可读取的具体建议。本次依据用户消息、Git 状态、内容清单与真实磁盘统计确定边界。

## 删除前审计

删除候选共 10,594 个文件、12,215,605,592 字节（约 11.377GiB）。

| 相对路径 | 文件数 | 字节 | 原因 |
|---|---:|---:|---|
| `artifacts/archive` | 6,827 | 9,123,748,190 | 多代完整包、旧 ZIP、WebView2 缓存和解压副本，已被 Git 历史和最终 XY 包取代 |
| `artifacts/publish/孤独摇滚壁纸移植-完整包` | 792 | 831,761,917 | 旧品牌发布目录 |
| `artifacts/release/孤独摇滚壁纸移植-完整包.zip` | 1 | 722,316,442 | 旧品牌 Release ZIP |
| `artifacts/smoke` | 80 | 15,202,835 | 可重复生成的截图、DOM、host 与 trace 报告 |
| `artifacts/verification` | 618 | 820,815,431 | 最终 ZIP 解包副本和包外自检临时目录 |
| `data` | 637 | 67,798,596 | WebView2 运行与 capture 缓存 |
| `logs` | 1 | 719 | 可再生成的自检日志 |
| `.packages` | 730 | 357,008,121 | 项目级 NuGet 缓存，可由 restore 恢复 |
| `.dotnet-home` | 103 | 95,197,055 | 项目级 .NET CLI 缓存 |
| `src/**/bin,obj` | 709 | 180,069,983 | 可由 build/publish 恢复的编译产物 |
| `tests/**/bin,obj` | 96 | 1,686,303 | 可由测试构建恢复的编译产物 |

## 明确保留

- `.git` 与全部本地提交/标签。
- `content/reference-player` 完整素材源。
- `manifests/reference-player.manifest.json`。
- `artifacts/publish/XY桌面播放器-v1.0.0-win-x64`，供用户手动打开测试。
- `artifacts/release/XY桌面播放器-v1.0.0-win-x64.zip`。
- 最终生成的 `artifacts/release/XY桌面播放器-GitHub源码-v1.0.0.zip`。
- 任务根目录 `work/tooling` 中的 .NET SDK，便于后续继续构建。
- 其他鼠标主题原型与分析素材；它们不是本桌面播放器的重复发布物。

## 安全措施

删除命令在单个 PowerShell 进程内执行。每个目标先解析为绝对路径并验证位于本项目根目录下；拒绝项目根、`.git`、`content`、`manifests`、新发布目录、新 Release ZIP。未使用通配符、环境变量拼接目标或跨 Shell 删除。

## 删除后结果

- 上表列出的 15 个实际目标（其中源码与测试的 `bin`、`obj` 分开计数）均已删除，全部复查为不存在。
- 第二轮缓存清理结束时，项目目录由约 11.22GiB 降至 2.055GiB：现有 1,489 个文件、2,206,541,679 字节。
- 同一时点整个任务根目录有 7,120 个文件、2,999,701,665 字节（约 2.794GiB）。
- 相比最初项目快照约释放 9.16GiB；本轮首批精确删除目标合计 12,215,605,592 字节（约 11.377GiB），其间已生成新的最终发布包。
- `content/reference-player`、内容清单、最终发布目录、最终 Release ZIP、`.git` 与任务根目录的 `work/tooling` 均仍存在。
- 最终发布目录仍为 617 个文件、820,814,574 字节；内容清单仍为 144 个文件、651,461,388 字节，逐项 SHA-256 一致。
- `XY桌面播放器-v1.0.0-win-x64.zip` 清理前后 SHA-256 均为 `BEA11CDBCAD927C19F1282DDD4504B9EC89829D3BDA1EE4E09087A5374DC539F`。
- 打标签前的最后一次完整复验曾重新生成 1,459 个依赖、编译与验收缓存文件，共 630,338,455 字节；测试结束后按同一保护边界再次全部删除，最终目录未夹带这些缓存。

上述删除是永久删除，没有进入回收站。源码、依赖缓存和编译产物可从 Git 标签、保留的素材与构建工具重新生成；已删除的旧品牌二进制归档不作为独立恢复点保留。如手动验收发现功能问题，应先从 `v1.0.0` 标签或对应提交建立修复分支，不要用破坏性重置覆盖当前工作。
